version: "3.8"

services:

  # openjdk:
    # build: .
    # image: openjdk
    # restart: always
    # container_name: allure_result
    # # ports:
    #   - "4444:4444"
    # logging:
    #   driver: local

  node:
    build: .
    image: node
    container_name: mocka_test
    # ports:
    #   - "5555:5555"
    # network_mode: "host"
    # logging:
    #   driver: local
    command: 
    # - |
      npx wdio run ./config/wdio.conf.js --suite positiveAndNegativeLogin 
      # npx allure serve
